# transformer-from-scratch

A decoder-only transformer (GPT-style) built in pure NumPy. Manual
backprop for every operation — embeddings, multi-head attention,
LayerNorm, FFN, LM head, and softmax cross-entropy.

Status: scaffolding.
